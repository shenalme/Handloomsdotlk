3D DIGITAL TWIN

handloom-01.glb is the project-provided 3D model used by the website's interactive viewer.

The GLB is intentionally lazy-loaded. The website does not download the 3D viewer library or this model on initial page load. The download begins only when a visitor selects “Load 3D twin”.

GitHub Pages path:
assets/models/handloom-01.glb
